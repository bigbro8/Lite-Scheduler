const Courses = require("../models/Courses");
const Courses = require("../models/Teachers");
const Courses = require("../models/Schedules");


const createTeacherInfo = async (req,res) =>{
    try {

    } catch {
        
    }
}